version: '3'
services:
  bot:
    build:
      context: .
    command: /bin/sh -c "python3 -m hxavatars.web"
    image: hxavatarsbot
    restart: unless-stopped
    tty: true
    volumes:
      - ./.env:/app/.env
      - ./shared:/app/data
